<Window x:Class="KeycardMenagmentSystem.View.ManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeycardMenagmentSystem.View"
        mc:Ignorable="d"
        Title="ManagerView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.RowSpan="4" Grid.Column="0" Grid.Row="0" Style="{StaticResource NavigationDockPanel}">
            <StackPanel>
                <Button Content="See all aceess points"/>
                <Button Content="See all cards" Style="{StaticResource NavigationButtonClicked}" Click="SeeAllCards"/>
            </StackPanel>
        </DockPanel>
        <Grid Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4" Grid.RowSpan="4" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <ListView Grid.Row="0"/>
            <Button Content="Generate report" Grid.Row="1" HorizontalAlignment="Right" Background="Blue" Foreground="White" FontSize="20"
                Margin="10" Padding="10 5 10 5" Click="GenerateReport"/>
        </Grid>
    </Grid>
</Window>
